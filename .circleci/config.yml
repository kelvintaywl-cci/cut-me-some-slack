version: 2.1

jobs:
  test:
    docker:
      - image: cimg/base:current
    resource_class: small
    steps:
      - run: printenv CIRCLE_NODE_INDEX

workflows:
  approval-required:
    jobs:
      - hold:
          type: approval
          requires:
           - test
